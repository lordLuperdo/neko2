export default{
    PORT: process.env.PORT || 3000,
    DB_HOST:process.env.DT_HOST ||'localhost',
    DB_USER:process.env.DB_USER || 'root',
    DB_PASSWORD:process.env.DT_PASSWORD || 'Conejito234*',
    DATBASE:process.env.DATABASE || 'nekko',
    DB_PORT:process.env.DB_PORT || 3306

}